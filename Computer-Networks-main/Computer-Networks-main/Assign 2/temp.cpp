#include <bits/stdc++.h>
#include <sys/stat.h>
#include <sys/types.h>
#include <unistd.h>
#include <fcntl.h>
using namespace std;

int main(){
	int a,b;
	cin >> a >> b;
	cout << a+b << endl;
	cin >> a >> b;
	cout << a-b << endl;
	cin >> a >> b;
	cout << a*b << endl;
	return 0;
}
